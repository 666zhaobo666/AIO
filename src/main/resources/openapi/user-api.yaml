openapi: 3.0.3
info:
  title: User Management API
  version: 1.0.0
  x-java-interface-name: UserApi
paths:
  /api/users/register:
    post:
      operationId: register
      summary: 用户注册
      tags:
        - User
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRegisterRequest'
      responses:
        '201':
          description: 注册成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserApiResponse'

  /api/users/login:
    post:
      operationId: login
      summary: 用户登录（支持用户名/邮箱/手机号）
      tags:
        - User
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserLoginRequest'
      responses:
        '200':
          description: 登录成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'

  /api/users/password:
    put:
      operationId: updatePassword
      summary: 修改密码（需登录）
      tags:
        - User
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePasswordRequest'
      responses:
        '200':
          description: 密码修改成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserApiResponse'

  /api/users/delete/{userId}:
    delete:
      operationId: deleteUser
      summary: 删除用户（管理员可删除所有用户，普通用户仅可删除自己）
      tags:
        - User
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: 删除成功

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    UserRegisterRequest:
      type: object
      required:
        - username
        - name
        - email
        - password
      properties:
        username:
          type: string
          maxLength: 100
        name:
          type: string
          maxLength: 100
        email:
          type: string
          maxLength: 100
        phone:
          type: string
          maxLength: 100
        password:
          type: string
          maxLength: 100
        gender:
          type: string
          default: U
        birthday:
          type: string
          format: date
        occupation:
          type: string
        signature:
          type: string

    UserLoginRequest:
      type: object
      required:
        - account
        - password
      properties:
        account:
          type: string # 用户名/邮箱/手机号
        password:
          type: string
          format: password

    LoginResponse:
      type: object
      properties:
        token:
          type: string
          description: JWT令牌
        userId:
          type: string
          format: uuid
        username:
          type: string
        role:
          type: string

    UpdatePasswordRequest:
      type: object
      required:
        - oldPassword
        - newPassword
      properties:
        oldPassword:
          type: string
          format: password
        newPassword:
          type: string
          minLength: 8
          format: password

    UserApiResponse:
      type: object
      properties:
        code:
          type: integer
          description: 状态码
        message:
          type: string
          description: 提示信息
        data:
          type: object